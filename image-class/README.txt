* To run, first install the environment found in environment.yaml$

conda env create -f environment.yaml


* Then activate the environment:

source activate dl

* Then open jupyter notebooks:

jupyter notebook
